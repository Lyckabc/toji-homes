# ─────────────────────────────────────────────────────────────────────────────
# Docker Build & Push — 비활성화
#
# 배포 파이프라인은 Woodpecker CI (.woodpecker.yml) 로 이전되었습니다.
# 이 워크플로우는 더 이상 실행되지 않습니다.
# ─────────────────────────────────────────────────────────────────────────────

name: Docker Build & Push (disabled)

on:
  workflow_dispatch:  # 수동 실행만 허용 (자동 트리거 없음)

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Deployment is handled by Woodpecker CI."
